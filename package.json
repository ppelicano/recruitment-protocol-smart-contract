{
  "name": "ens-subdomain-factory",
  "author": "Pedro Pelicano",
  "version": "1.0.0",
  "description": "Recruitment Protocol Smart Contract",
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "truffle migrate --reset --network goerli",
    "build": "truffle compile"
  },
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "requires": true,
  "lockfileVersion": 1,
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.0",
    "eth-ens-namehash-ms": "^2.2.0",
    "truffle": "^5.6.5",
    "truffle-hdwallet-provider": "^1.0.17",
    "web3": "^1.0.0-beta.35"
  }
}
